name: TimeMachine
main: cn.ruofengx.TimeMachine.TimeMachine
version: ${project.version}
author: 黄金鞘翅的郡主
commands:
   timemachine:
      description: 时间机器
      usage: /timemachine autoupload <on|off>
      # permission: <plugin name>.basic
      # permission-message: You don't have <permission>